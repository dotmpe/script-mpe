#!/usr/bin/env bash

SCRIPTPATH="$SCRIPTPATH:$(dirname "${BASH_SOURCE[0]}")"
SCRIPTPATH="$SCRIPTPATH:$(dirname "${BASH_SOURCE[0]}")/commands"
SCRIPTPATH="$SCRIPTPATH:$(dirname "${BASH_SOURCE[0]}")/contexts"
