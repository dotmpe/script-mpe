- Support strict and loose dot-extension name schemes for files with
  registry of extensions - mediatype mappings.

- MIME's can have multiple distinct formats,
  and detection is not always correct, making extension-mediatype mapping more
  difficult.
