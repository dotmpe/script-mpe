sudo: false

install: ./install-dependencies.sh

before_script:
- 'pip install --user jsonschema'
- 'export PATH=$PATH:./'

script: make -k test

branches:
  only:
  - master
#  - dev_taxus
#  - '/^[0-9]*\.[0-9]*\.[0-9]*/'

env:
- ENV= CS=dark
#- ENV=testing
#- ENV=production

