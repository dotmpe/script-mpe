

# Install redo
# Breaks on docker exec, or after git clean -dfx?
RUN \
  git clone https://github.com/apenwarr/redo.git /src/github.com/apenwarr/redo && \
  cd /src/github.com/apenwarr/redo && ./redo install || true

