language: none

git:
  submodules: true

build:
  pre_ci_boot:
    image_name: dotmpe/testbox
    image_tag: latest # 0.0.4-bionic
    pull: true

  ci:
    - export TERM=xterm U_S=$HOME/build/dotmpe/user-scripts; ./sh-ci

# Sync: U-S:shippable.yml
